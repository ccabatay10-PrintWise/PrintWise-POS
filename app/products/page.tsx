"use client";

import { useEffect, useMemo, useState } from "react";
import { ArrowLeft, Package, Plus, Search, Pencil, Power, ReceiptText, X, Save, ImagePlus } from "lucide-react";
import { supabase } from "../../lib/supabase";
import "../pos/pos.css";
import Sidebar from "../components/Sidebar";

type Product = {
  id: string; sku: string | null; name: string; category: string; description: string | null;
  unit: string; price: number; icon_key: string; image_url: string | null; is_active: boolean;
};
type ProductForm = { sku: string; name: string; category: string; description: string; unit: string; price: string; icon_key: string; image_url: string; };
const emptyForm: ProductForm = { sku: "", name: "", category: "", description: "", unit: "piece", price: "", icon_key: "box", image_url: "" };

export default function ProductsPage() {
  const [products, setProducts] = useState<Product[]>([]); const [search, setSearch] = useState(""); const [loading, setLoading] = useState(true);
  const [message, setMessage] = useState(""); const [saving, setSaving] = useState(false); const [modalOpen, setModalOpen] = useState(false);
  const [editingId, setEditingId] = useState<string | null>(null); const [form, setForm] = useState<ProductForm>(emptyForm); const [imageFile, setImageFile] = useState<File | null>(null);
  const loadProducts = async () => { setLoading(true); const { data, error } = await supabase.from("products").select("id,sku,name,category,description,unit,price,icon_key,image_url,is_active").order("category").order("name"); if (error) setMessage(`Unable to load products: ${error.message}`); else setProducts((data ?? []).map((p:any) => ({ ...p, price: Number(p.price) }))); setLoading(false); };
  useEffect(() => { supabase.auth.getUser().then(({ data }) => { if (!data.user) window.location.href = "/pos"; else loadProducts(); }); }, []);
  const filtered = useMemo(() => products.filter(p => `${p.name} ${p.category} ${p.sku || ""}`.toLowerCase().includes(search.toLowerCase())), [products, search]);
  const openAdd = () => { setEditingId(null); setForm(emptyForm); setImageFile(null); setMessage(""); setModalOpen(true); };
  const openEdit = (p: Product) => { setEditingId(p.id); setImageFile(null); setForm({ sku:p.sku||"", name:p.name, category:p.category, description:p.description||"", unit:p.unit||"piece", price:String(p.price), icon_key:p.icon_key||"box", image_url:p.image_url||"" }); setMessage(""); setModalOpen(true); };
  const handleImage = (file?: File) => { if (!file) return; if (!file.type.startsWith("image/")) { setMessage("Please choose a valid image file."); return; } if (file.size > 5 * 1024 * 1024) { setMessage("Image must be 5MB or smaller."); return; } setImageFile(file); setForm(f => ({ ...f, image_url: URL.createObjectURL(file) })); };
  const saveProduct = async () => {
    const name=form.name.trim(), category=form.category.trim(), price=Number(form.price); if (!name || !category || form.price==="" || Number.isNaN(price) || price<0) { setMessage("Please enter a product name, category, and valid price."); return; }
    setSaving(true); setMessage(""); let imageUrl = form.image_url || null;
    if (imageFile) { const ext=imageFile.name.split(".").pop() || "jpg"; const safeName=`${Date.now()}-${crypto.randomUUID()}.${ext}`; const { error: uploadError } = await supabase.storage.from("product-images").upload(safeName, imageFile, { upsert:false, contentType:imageFile.type }); if (uploadError) { setMessage(`Unable to upload image: ${uploadError.message}`); setSaving(false); return; } const { data } = supabase.storage.from("product-images").getPublicUrl(safeName); imageUrl=data.publicUrl; }
    const payload={ sku:form.sku.trim()||null, name, category, description:form.description.trim()||null, unit:form.unit.trim()||"piece", price, icon_key:form.icon_key.trim()||"box", image_url:imageUrl };
    const result=editingId ? await supabase.from("products").update(payload).eq("id",editingId) : await supabase.from("products").insert({ ...payload, is_active:true });
    if (result.error) { setMessage(`Unable to save product: ${result.error.message}`); setSaving(false); return; }
    setModalOpen(false); setEditingId(null); setForm(emptyForm); setImageFile(null); setSaving(false); setMessage(editingId ? "Product updated successfully." : "Product added successfully."); await loadProducts();
  };
  const toggleStatus=async(p:Product)=>{setMessage("");const{error}=await supabase.from("products").update({is_active:!p.is_active}).eq("id",p.id);if(error){setMessage(`Unable to change status: ${error.message}`);return;}setProducts(c=>c.map(x=>x.id===p.id?{...x,is_active:!p.is_active}:x));setMessage(`${p.name} is now ${p.is_active?"Inactive":"Active"}.`);};
  return <main className="app-shell"><Sidebar /><section className="workspace"><header className="topbar"><div><h1>Products & Services</h1><p>Manage the products and services available in PrintWise POS.</p></div><button className="process-btn" style={{width:"auto"}} onClick={openAdd}><Plus size={18}/> ADD PRODUCT</button></header><div className="pos-layout" style={{gridTemplateColumns:"1fr"}}><section className="catalog-panel"><div className="search-box"><Search size={19}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search products and services..."/></div>{message&&<div className="message">{message}</div>}<div style={{overflowX:"auto",marginTop:18}}><table className="orders-table"><thead><tr><th>Product / Service</th><th>Category</th><th>Price</th><th>Status</th><th>Action</th></tr></thead><tbody>{loading?<tr><td colSpan={5}>Loading products...</td></tr>:filtered.length===0?<tr><td colSpan={5}>No products found.</td></tr>:filtered.map(p=><tr key={p.id}><td><div style={{display:"flex",alignItems:"center",gap:10}}>{p.image_url?<img src={p.image_url} alt="" style={{width:42,height:42,objectFit:"cover",borderRadius:8,border:"1px solid #e2e8f0"}}/>:<div className="product-icon" style={{width:42,height:42}}><Package size={18}/></div>}<div><b>{p.name}</b>{p.sku&&<small style={{display:"block",opacity:.65,marginTop:3}}>SKU: {p.sku}</small>}</div></div></td><td>{p.category}</td><td>₱{p.price.toFixed(2)}</td><td><span className="order-status">{p.is_active?"Active":"Inactive"}</span></td><td><button className="icon-btn" title="Edit product" onClick={()=>openEdit(p)}><Pencil size={16}/></button>{" "}<button className="icon-btn" title={p.is_active?"Deactivate product":"Activate product"} onClick={()=>toggleStatus(p)}><Power size={16}/></button></td></tr>)}</tbody></table></div></section></div></section>{modalOpen&&<div style={{position:"fixed",inset:0,background:"rgba(0,0,0,.45)",display:"flex",alignItems:"center",justifyContent:"center",padding:20,zIndex:1000}}><div style={{width:"min(680px,100%)",maxHeight:"90vh",overflowY:"auto",background:"white",borderRadius:18,padding:24,boxShadow:"0 20px 60px rgba(0,0,0,.25)"}}><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:18}}><div><h2 style={{margin:0}}>{editingId?"Edit Product":"Add Product"}</h2><p style={{margin:"6px 0 0",color:"#64748b"}}>Save this product directly to your PrintWise database.</p></div><button className="icon-btn" onClick={()=>setModalOpen(false)}><X size={18}/></button></div><div style={{display:"grid",gridTemplateColumns:"repeat(2,minmax(0,1fr))",gap:14}}><label style={{gridColumn:"1 / -1",display:"grid",gap:6}}>Product / Service Name<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="e.g. Ballpen Printing"/></label><label style={{display:"grid",gap:6}}>Category<input value={form.category} onChange={e=>setForm({...form,category:e.target.value})} placeholder="e.g. Printing"/></label><label style={{display:"grid",gap:6}}>Price<input type="number" min="0" step="0.01" value={form.price} onChange={e=>setForm({...form,price:e.target.value})} placeholder="0.00"/></label><label style={{display:"grid",gap:6}}>SKU (optional)<input value={form.sku} onChange={e=>setForm({...form,sku:e.target.value})} placeholder="Optional SKU"/></label><label style={{display:"grid",gap:6}}>Unit<input value={form.unit} onChange={e=>setForm({...form,unit:e.target.value})} placeholder="piece"/></label><label style={{gridColumn:"1 / -1",display:"grid",gap:6}}>Product Sample Picture<input type="file" accept="image/*" onChange={e=>handleImage(e.target.files?.[0])}/>{form.image_url&&<div style={{display:"flex",alignItems:"center",gap:12}}><img src={form.image_url} alt="Product preview" style={{width:100,height:100,objectFit:"cover",borderRadius:10,border:"1px solid #e2e8f0"}}/><button type="button" className="icon-btn" style={{width:"auto",padding:"8px 12px"}} onClick={()=>{setImageFile(null);setForm(f=>({...f,image_url:""}))}}><X size={16}/> Remove</button></div>}</label><label style={{gridColumn:"1 / -1",display:"grid",gap:6}}>POS Icon<select value={form.icon_key} onChange={e=>setForm({...form,icon_key:e.target.value})}><option value="box">Box / Default</option><option value="printer">Printer</option><option value="pen">Pen</option><option value="paper">Paper</option><option value="id">ID / Card</option><option value="layers">Laminating</option><option value="mug">Mug</option><option value="photo">Photo</option><option value="shirt">Shirt</option><option value="sticker">Sticker</option></select></label><label style={{gridColumn:"1 / -1",display:"grid",gap:6}}>Description (optional)<textarea value={form.description} onChange={e=>setForm({...form,description:e.target.value})} rows={3} placeholder="Optional product description"/></label></div><div style={{display:"flex",justifyContent:"flex-end",gap:10,marginTop:22}}><button className="icon-btn" style={{padding:"10px 16px",width:"auto"}} onClick={()=>setModalOpen(false)}>Cancel</button><button className="process-btn" style={{width:"auto",padding:"10px 18px"}} disabled={saving} onClick={saveProduct}><Save size={17}/> {saving?"SAVING...":editingId?"SAVE CHANGES":"ADD PRODUCT"}</button></div></div></div>}</main>;
}